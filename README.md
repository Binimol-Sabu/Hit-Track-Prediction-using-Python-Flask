# Hit Track Prediction with Flask

A web app that predicts song popularity and classifies tracks as "Hit" or "Non-Hit" using machine learning.

## 🚀 How to Run

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   
